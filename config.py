
class config:

    player = "X" # Player can either be "X" or "O"

    #set the pygame window size (width, height) in pixels
    WINDOW_SIZE = [500, 500]

    # number of rows and columns, would advise 3x3
    ROWS, COLS = 3, 3

    # Set the margin between each cell (cosmetic)
    MARGIN = 1

    # Set the colors - FIXME: Not currnetly used, but could be used to change the color of the grid
    BLACK = (0, 0, 0)
    WHITE = (255, 255, 255)
    RED = (255, 0, 0)
